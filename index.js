import { registerRootComponent } from 'expo';
import App from './App';

// DO NOT import or reference react-native-track-player here
// Use expo-av instead (already included in Expo)

registerRootComponent(App);